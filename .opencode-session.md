# Sesión 28-29 Julio 2026 — Webautomatizaciones

## ✅ Arreglado

### Chatbot (502 Bad Gateway)
- `chatController.js`: validación si OMNIROUTE_URL no está configurado → mensaje amigable
- Timeout 15s al fetch de OmniRoute (AbortController)
- Errores devuelven `{ reply, leadCaptured }` en vez de `{ error }`
- API key configurable via `OMNIROUTE_API_KEY` (fallback: "omniroute")

### Formulario de contacto
- Campos separados: `name`, `email`, `business`, `phone`, `message`
- Validación en backend para cada campo
- Email HTML estructurado
- `contactController.js` ahora usa el `mailer.js` compartido (no duplicado)

### Mailer (`mailer.js`)
- Si SMTP_HOST no está configurado → devuelve `null`, no crashea
- Tanto chatbot como formulario detectan `!transporter` y avisan sin error

### Otros
- CORS: `localhost:5173` → `5175` (coincide con Vite)
- Email inconsistente: About.jsx unificado a `paulact39@gmail.com`
- Services.jsx añadido al router (`/servicios`) y navbar
- ChatWidget: `renderText` protegido contra null/undefined
- Chat movido a esquina inferior izquierda (`left: 1.5rem`)
- CRM imagen más grande: `max-width: 1200px→1400px`, `scale(1.15)`
- `render.yaml`: añadidas variables `OMNIROUTE_API_KEY` y comentarios

## 🔄 OmniRoute — Pendiente

### Estado actual
- ✅ Instalado local: `npm install -g omniroute`
- ✅ Corriendo local: `http://localhost:20128` (PID activo via setsid)
- ✅ API key generada: `sk-c7ca9f537a2ee9cf-e773ed-d7021339`
- ✅ Password setup: `changeme` (cambiado en dashboard)
- ❌ Proveedores conectados pero no funcionales:
  - `agy` (Antigravity): necesita reconectar OAuth Google
  - `github` (GitHub Copilot): modelos no soportados en free
- ❌ Render deploy falla: exit code 128 (elegir "Docker Image", no "Web Service")

### Pasos para mañana
1. Borrar servicio `omniroute` en Render
2. Crear nuevo: **New+ → Web Service → "Deploy from Docker Hub"**
3. Imagen: `diegosouzapw/omniroute:latest-web`
4. Env var: `OMNIROUTE_WS_BRIDGE_SECRET=webautomatizaciones-secret-2026`
5. Health check: `/v1/models`
6. Una vez desplegado, abrir `https://omniroute.onrender.com`
7. Login con `changeme`, conectar **OpenCode Free** (Providers → Add Provider)
8. En servicio `webautomatizaciones` → Environment, añadir:
   - `OMNIROUTE_URL=https://omniroute.onrender.com/v1`
   - `OMNIROUTE_API_KEY=sk-c7ca9f537a2ee9cf-e773ed-d7021339`
9. Crear cuenta en cron-job.org para ping cada 14min a `https://omniroute.onrender.com/v1/models`
10. Hacer deploy de `webautomatizaciones`

### Comandos útiles
```bash
# Iniciar OmniRoute local
export PATH="$HOME/.npm-global/bin:$PATH"
setsid omniroute --no-open > /tmp/omniroute.log 2>&1 &

# Verificar
curl http://localhost:20128/v1/models -H "Authorization: Bearer sk-c7ca9f537a2ee9cf-e773ed-d7021339"

# Probar chat
curl http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer sk-c7ca9f537a2ee9cf-e773ed-d7021339" \
  -H "Content-Type: application/json" \
  -d '{"model":"auto/best-free","messages":[{"role":"user","content":"OK"}],"max_tokens":5}'
```
